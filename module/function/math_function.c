#include <stdio.h>
#include <math.h>
#include <stdlib.h>
// ceil, floor, round, squar, pow
int main()
{

    // double x;
    // scanf("%lf", &x);
    // ceil
    // int ans = ceil(x);
    // printf("%d", ans);

    // floor
    // int ans = floor(x);
    // printf("%d", ans);

    // round
    // int ans = round(x);
    // printf("%d", ans);

    // sqrt
    // int ans = sqrt(x);
    // printf("%d", ans);

    // pow
    //    double a,b;
    //     scanf("%lf %lf", &a, &b);
    //     int ans = pow(a,b);
    //     printf("%d", ans);

    // abs mane negitive ke positive kore dey
    int a;
    scanf("%d", &a);
    int ans = abs(a);
    printf("%d", ans);

    return 0;
}